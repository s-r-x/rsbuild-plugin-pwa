# Dev mode

Since `v0.3.0` the plugin works with `rsbuild dev` command. 
To enable it just pass `true` to the [dev config](/api/interfaces/PWAPluginOptions.html#dev).
Keep in mind that the generated SW doesn't precache anything in dev mode.
