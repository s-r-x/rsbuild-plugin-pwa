# Precaching
The plugin [overrides](/api/interfaces/index.GenerateSwModeConfig.html#include) default workbox-build **globPatterns** value, and precaches only the assets generated by rsbuild plus several SW related artifacts (like web app manifest). This behavior is customizable in the [config](/api/interfaces/index.GenerateSwModeConfig.html#include).

