# Precaching
The plugin [overrides](/api/interfaces/GenerateSwModeConfig.html#include) default workbox-build **globPatterns** value, and precaches only the assets generated by rsbuild plus several SW related artifacts (like web app manifest). This behavior is customizable via the [config](/api/interfaces/GenerateSwModeConfig.html#include).

